<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      {{#linkTo app.home class="brand"}}
        {{unbound App.name}}
      {{/linkTo}}
      <div class="nav-collapse collapse">
        <ul class="nav">
          {{#linkTo "app.home" tagName="li"}}
            <a {{bindAttr href="view.href"}}>Home</a>
          {{/linkTo}}
          {{#linkTo "app.about" tagName="li"}}
            <a {{bindAttr href="view.href"}}>About</a>
          {{/linkTo}}
          {{#linkTo "app.contact" tagName="li"}}
            <a {{bindAttr href="view.href"}}>Contact</a>
          {{/linkTo}}
          <li class="dropdown visible-phone">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              {{App.author}}
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>
                <a href="#">Logout</a>
              </li>
            </ul>
          </li>
        </ul>
        <div class="muted pull-right dropdown hidden-phone">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="icon-user icon-white"></i>
            {{App.author}}
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <a href="#">Logout</a>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>